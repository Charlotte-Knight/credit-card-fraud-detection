apiVersion: 1

providers:
  - name: ccfd # A uniquely identifiable name for the provider
    folder: ccfd # The folder where to place the dashboards
    type: file
    options:
      path:
        /etc/grafana/provisioning/dashboards/